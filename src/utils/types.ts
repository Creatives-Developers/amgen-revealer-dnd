export type Cloud = {
  key: string;
  top: string;
  mobileTop?: string;
  left: string;
  width: string;
  imageName: string;
};

export type DraggableItem = {
  key: string;
};

export type DragableItemPosition = { x: number; y: number };
